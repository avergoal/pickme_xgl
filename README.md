# PickMe XGL

Free-to-play X Games / XGL pick'em MVP for action sports fans.

## Preview locally

Open `index.html` directly in a browser, or run a tiny local server:

```bash
python3 -m http.server 4256
